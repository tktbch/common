{
  "name": "@tktbitch/common",
  "version": "1.0.1",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "license": "MIT",
  "scripts": {
    "clean": "del ./build/*",
    "build": "yarn clean && tsc",
    "pub": "git add . && git commit -m \"Some updates\" && npm version patch && yarn build && npm publish"
  },
  "devDependencies": {
    "del-cli": "^3.0.1",
    "typescript": "^3.9.3"
  }
}
